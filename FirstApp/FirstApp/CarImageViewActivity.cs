using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Webkit;
using Android.Widget;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
/*
namespace FirstApp
{
    WebView _webView;
    [Activity(Label = "CarImageViewActivity")]
    public class CarImageViewActivity : Activity
    {
        base.OnCreate(savedInstanceState);
        SetContentView(Resource.Layout.);

        // Create your application here
        var address = Intent.GetStringExtra(Constants.AddressKey);
        _webView = FindViewById<WebView>(Resource.Id.webView1);
            _webView.Settings.JavaScriptEnabled = true;
            _webView.SetWebViewClient(new SimpleWebViewClient());
            _webView.LoadUrl(address);
        
        protected override void OnCreate(Bundle savedInstanceState)
        {
            base.OnCreate(savedInstanceState);

            // Create your application here
        }
    }
}*/